// Stable PiDeck built-in entrypoint. The implementation lives in a sibling
// directory so its provider modules stay grouped and package together.
export { default } from "./pideck-q-websearch/extension-runtime.ts";
