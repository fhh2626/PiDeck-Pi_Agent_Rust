// PiDeck-bundled entrypoint for @lll9p/pi-better-compaction v0.2.1.
// The implementation stays in a sibling directory so its relative imports
// remain intact when the extension is loaded by pi from extraResources.
export { default } from "./pi-better-compaction/extension-runtime.ts";
